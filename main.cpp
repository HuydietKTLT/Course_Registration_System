#include "password.h"
#include "staff.h"

int main()
{
    //string ID = "";
    //char type;
    //login(type, ID);
    /*if (ID != "")
        edit(ID);*/
    //if (type == 'm')
        menuStaff();
    return 0;
}

